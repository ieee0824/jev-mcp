interface:
  display_name: "Decision Support with Jev"
  short_description: "Assess coding decisions with the Jev MCP server"

dependencies:
  tools:
    - type: "mcp"
      value: "jev"
      description: "Configured Jev stdio MCP server"
